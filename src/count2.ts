export function pass(num) {
    if (num > 60) {
        return true;
    }
    return false;
};

export function max(num1, num2) {
    return num1 > num2 ? num1 : num2;
};
// hello
const aa = "24";
console.log(aa);
